{
  "name": "@ygoma/main",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev --host 0.0.0.0",
    "build": "vite build",
    "build:preview": "NITRO_PRESET=cloudflare-pages vite build",
    "cf:preview": "bun run build:preview && wrangler pages dev --log-level=error dist",
    "cf:db:migrate": "wrangler d1 migrations apply dev --local",
    "cf:db:seed": "wrangler d1 execute dev --local --file=seed.sql",
    "cf:db:reset": "rimraf .wrangler/state && bun cf:db:migrate && bun cf:db:seed",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "bun db/seed.ts",
    "db:reset": "rimraf sqlite.db && bun db:migrate && bun db:seed",
    "lint": "oxfmt -c ../../.oxfmtrc.json --check . && oxlint -c ../../.oxlintrc.json && tsc -b"
  },
  "dependencies": {
    "@libsql/client": "catalog:",
    "@ygoma/ui": "workspace:*",
    "drizzle-orm": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@sentry/cloudflare": "catalog:",
    "@sentry/core": "catalog:",
    "@sentry/react": "catalog:",
    "@sentry/vite-plugin": "catalog:",
    "@tanstack/react-router": "catalog:",
    "@tanstack/react-start": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "@ygoma/tsconfig": "workspace:*",
    "bun-types": "catalog:",
    "drizzle-kit": "catalog:",
    "nitro": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "wrangler": "catalog:"
  }
}
